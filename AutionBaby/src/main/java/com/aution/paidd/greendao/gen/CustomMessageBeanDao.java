package com.aution.paidd.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.aution.paidd.bean.CustomMessageBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CUSTOM_MESSAGE_BEAN".
*/
public class CustomMessageBeanDao extends AbstractDao<CustomMessageBean, Long> {

    public static final String TABLENAME = "CUSTOM_MESSAGE_BEAN";

    /**
     * Properties of entity CustomMessageBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "id", true, "_id");
        public final static Property Url = new Property(1, String.class, "url", false, "URL");
        public final static Property Action = new Property(2, String.class, "action", false, "ACTION");
        public final static Property Msgid = new Property(3, String.class, "msgid", false, "MSGID");
    }


    public CustomMessageBeanDao(DaoConfig config) {
        super(config);
    }
    
    public CustomMessageBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CUSTOM_MESSAGE_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: id
                "\"URL\" TEXT," + // 1: url
                "\"ACTION\" TEXT," + // 2: action
                "\"MSGID\" TEXT);"); // 3: msgid
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CUSTOM_MESSAGE_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CustomMessageBean entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String url = entity.getUrl();
        if (url != null) {
            stmt.bindString(2, url);
        }
 
        String action = entity.getAction();
        if (action != null) {
            stmt.bindString(3, action);
        }
 
        String msgid = entity.getMsgid();
        if (msgid != null) {
            stmt.bindString(4, msgid);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CustomMessageBean entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String url = entity.getUrl();
        if (url != null) {
            stmt.bindString(2, url);
        }
 
        String action = entity.getAction();
        if (action != null) {
            stmt.bindString(3, action);
        }
 
        String msgid = entity.getMsgid();
        if (msgid != null) {
            stmt.bindString(4, msgid);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public CustomMessageBean readEntity(Cursor cursor, int offset) {
        CustomMessageBean entity = new CustomMessageBean( //
            cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // url
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // action
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // msgid
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CustomMessageBean entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setUrl(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setAction(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setMsgid(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CustomMessageBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CustomMessageBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CustomMessageBean entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
